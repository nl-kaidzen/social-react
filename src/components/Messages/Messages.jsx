import React from 'react';
import style from './Messages.module.scss';

const Messages = (props) => {
    return(
        <div>
            Here will placed messages from users;
        </div>
    );
}

export default Messages;